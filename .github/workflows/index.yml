name: Generate PoC Index

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  generate-index:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout Index Repository
        # Checks out your repository (PoC-in-GitHub-index) into the default path
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Clone PoC-in-GitHub Repository
        # Clones the target repository into a subdirectory
        uses: actions/checkout@v4
        with:
          repository: nomi-sec/PoC-in-GitHub
          path: 'PoC-in-GitHub'
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate Index
        # The script is in the root and creates poc_index.json in the root
        run: python generate_index.py

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        id: commit
        with:
          commit_message: "🤖 Update PoC index [skip ci]"
          file_pattern: "poc_index.json"
          commit_user_name: GitHub Actions Bot
          commit_user_email: actions@github.com
          commit_author: GitHub Actions Bot <actions@github.com>
          # The working directory is the root, where the file was created
          # This is the crucial line that was missing or incorrect before
          # The default working directory is the root of the checked out repository
          # We just need to ensure the file is created there.

      - name: Create Release and Upload Asset
        if: steps.commit.outputs.changes_detected == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: index-v${{ github.run_number }}
          name: PoC Index Update ${{ github.run_number }}
          body: |
            Automated update of the PoC-in-GitHub index.
            This release contains the latest `poc_index.json` file.
          files: poc_index.json
